<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/intense_background"
    android:padding="24dp"
    tools:context=".ChangeCredentialsActivity">

    <TextView
        android:id="@+id/changeCredHeaderTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ubah Kredensial"
        android:textColor="@color/intense_text_primary"
        android:textSize="32sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.1"
        app:layout_constraintBottom_toTopOf="@+id/currentUsernameTextView"/>

    <TextView
        android:id="@+id/currentUsernameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:textColor="@color/intense_text_secondary"
        android:textSize="16sp"
        tools:text="Mengubah kredensial untuk: user@example.com"
        app:layout_constraintTop_toBottomOf="@id/changeCredHeaderTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <EditText
        android:id="@+id/newUsernameEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="Username Baru (opsional)"
        android:textColor="@color/intense_text_secondary"
        android:textColorHint="@color/intense_text_secondary"
        android:backgroundTint="@color/intense_text_primary"
        android:padding="12dp"
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@+id/currentUsernameTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/currentPasswordEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Password Saat Ini"
        android:textColor="@color/intense_text_secondary"
        android:textColorHint="@color/intense_text_secondary"
        android:backgroundTint="@color/intense_text_primary"
        android:inputType="textPassword"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@+id/newUsernameEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/newPasswordEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Password Baru (opsional)"
        android:textColor="@color/intense_text_secondary"
        android:textColorHint="@color/intense_text_secondary"
        android:backgroundTint="@color/intense_text_primary"
        android:inputType="textPassword"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@+id/currentPasswordEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/confirmNewPasswordEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Konfirmasi Password Baru (jika diisi)"
        android:textColor="@color/intense_text_secondary"
        android:textColorHint="@color/intense_text_secondary"
        android:backgroundTint="@color/intense_text_primary"
        android:inputType="textPassword"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@+id/newPasswordEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/saveChangesButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Simpan Perubahan"
        android:backgroundTint="@color/intense_text_primary"
        android:textColor="@color/intense_background"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/confirmNewPasswordEditText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>